{
  "name": "nodebootstraper",
  "version": "5.0.0",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "react-icons": "^5.2.1",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
